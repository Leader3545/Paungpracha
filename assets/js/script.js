document.addEventListener("DOMContentLoaded", function() {
  AOS.init({
    duration: 1000,
    once: true,
  });
});

function loadMoreNews() {
  // ตัวอย่างสำหรับการโหลดข่าวเพิ่มเติม (สามารถพัฒนาเป็น AJAX ได้ในอนาคต)
  alert("ฟังก์ชันโหลดข่าวเพิ่มเติมอยู่ระหว่างการพัฒนา");
}
